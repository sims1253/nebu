# nebu

nebu is the repository that stands in for the org. It holds the published projects.

The first is [janus](janus/README.md), a specification-driven PDF comparison and review tool. You upload a PDF, a JSON comparison specification, and reference data. Janus extracts the fields the specification names, compares them against the reference values, and serves a review UI over the results.
